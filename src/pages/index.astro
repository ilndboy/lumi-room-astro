---
import Layout from '../layouts/Layout.astro';
import Window from '../components/Window.astro';
import { getWindowData } from '../data/window';

const data = await getWindowData();
---

<Layout title="the window — lumi.stonemanor.us">
  <Window data={data} />
</Layout>